# #100Devs
## [Class 02](https://youtu.be/GATRO-33U54?si=BPLzeRt3_iGXH8um) notes

Agenda: 26:40

freelance job idea:
1. Cold-call restuarants around and ask if they would like a restuarant website for $XX (eg. $300). 

#### Shortcuts 
`command`+`right arrow` to jump to the end of a line;
`command`+`left arrow` to jump to the start of a line;

#### Notes:
`<img>` image tag doesn't have a closing tag. It is self-closing.
`<input>` input tag is also self-closing & doesn't have a closing tag.

`<!-- hint: <img> tag -->` this is a comment.

`<nav>` navigation tag.
`<form>` forms tag. `action` & `method` attributes.

### Progessive Enhancement